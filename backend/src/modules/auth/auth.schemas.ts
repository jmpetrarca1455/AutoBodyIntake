/**
 * Auth contracts — re-exported from @autobody/shared (single source of
 * truth used by both the backend and the customer/dashboard clients).
 */
export {
  signupSchema,
  loginSchema,
  createStaffSchema,
  type SignupInput,
  type LoginInput,
  type AuthResponse,
  type ShopRole,
  type CreateStaffInput,
  type StaffMember,
} from '@autobody/shared';


