{
  "name": "autobody-intake",
  "version": "0.1.0",
  "private": true,
  "description": "AutoBody Intake — monorepo (shared contracts, backend API, customer app)",
  "workspaces": [
    "packages/*",
    "backend",
    "app"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspace=@autobody/shared && npm run typecheck --workspace=backend && npm run typecheck --workspace=app",
    "build:shared": "npm run build --workspace=@autobody/shared",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:app": "npm run web --workspace=app"
  }
}

